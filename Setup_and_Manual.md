# คู่มือการติดตั้งและใช้งานระบบบริหารสัญญา (Contract Management System)
## ฝ่ายสาธารณูปโภค ส่วนการโยธา สำนักช่าง เทศบาลนครระยอง

---

### 🔐 1. ข้อมูลบัญชีผู้ใช้งานเริ่มต้น (Default Login Accounts)

เมื่อติดตั้งระบบครั้งแรก ระบบจะสร้างชีต `Users` ใน Google Sheets ให้อัตโนมัติ พร้อมบัญชีเริ่มต้นดังนี้:

| ระดับสิทธิ์ (Role) | ชื่อผู้ใช้งาน (Username) | รหัสผ่าน (Password) | ชื่อ-สกุล และตำแหน่ง | หน้าที่และสิทธิ์ |
| :--- | :--- | :--- | :--- | :--- |
| **Admin** 🛡️ | `admin` | `admin` | ผู้ดูแลระบบ สำนักช่าง | จัดการผู้ใช้ทั้งหมด, เพิ่ม/แก้ไขโครงการ, ดูแลระบบ |
| **User** 👷 | `somchai` | `1234` | นายสมชาย ใจมั่น (วิศวกรโยธาชำนาญการ) | บันทึกตรวจงานประจำวัน (Daily Log), ตรวจรับงาน |
| **User** 👷 | `somsak` | `1234` | นายสมศักดิ์ วงศ์ดี (นายช่างโยธาชำนาญงาน) | บันทึกตรวจงานประจำวัน (Daily Log), ตรวจรับงาน |
| **User** 👷 | `wichai` | `1234` | นายวิชัย ช่างทอง (นายช่างโยธาปฏิบัติงาน) | บันทึกตรวจงานประจำวัน (Daily Log), ตรวจรับงาน |

> 💡 **หมายเหตุ:** แอดมินสามารถเพิ่มหรือแก้ไขรายชื่อนายช่างผู้ควบคุมงานและรหัสผ่านได้ตลอดเวลา ผ่านเมนู **"จัดการผู้ใช้งาน (User Management)"** หรือแก้ไขในแผ่นงาน `Users` บน Google Sheets

---

### 📂 2. โครงสร้างชีตฐานข้อมูลใน Google Sheets (`Users`)
- คอลัมน์ A: `UserID` (เช่น USR-001)
- คอลัมน์ B: `Username` (เช่น admin, somchai)
- คอลัมน์ C: `Password` (รหัสผ่าน)
- คอลัมน์ D: `FullName` (ชื่อ-สกุล และตำแหน่ง)
- คอลัมน์ E: `Role` (Admin / User)
- คอลัมน์ F: `Department` (ฝ่ายสาธารณูปโภค ส่วนการโยธา สำนักช่าง)
- คอลัมน์ G: `Status` (Active / Inactive)
- คอลัมน์ H: `LastLogin` (วันเวลาเข้าใช้งานล่าสุด)

---

### 🚀 3. ระบบบันทึกผู้ปฏิบัติงานอัตโนมัติ (Audit Trail)
- เมื่อนายช่างเข้าสู่ระบบและกดบันทึกรายงานตรวจงานประจำวัน ระบบจะดึงชื่อ-สกุล และ Username ผู้บันทึกไปผูกกับรายงานนั้นโดยอัตโนมัติ ทำให้ผู้บริหารและคณะกรรมการสามารถตรวจสอบย้อนหลังได้ชัดเจน 100% ว่างานไหนใครเป็นผู้บันทึกและส่งรายงาน
